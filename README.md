# true-ai-export
Fix for Claude.ai web UI copy-paste regression. Exports full conversation transcripts via Anthropic's own API, bypassing the virtualized DOM rendering that broke clipboard copy, Save Page As, and Print to PDF. Markdown, plain text, HTML, JSON. No install.
